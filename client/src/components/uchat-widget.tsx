// UChatWidget is loaded globally via script tag in index.html
// This component is kept for consistency but does not interfere with UChatWidget
export default function WhatsAppWidget() {
  return null;
}